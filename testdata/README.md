# testdata

テストイメージの生成スクリプトを置く場所。生成物 (`out/`, `*.img`) は gitignore する。

- Phase 0 時点では中身なし。`MockDevice` によるエラー注入だけでテストしている。
- Phase 2 で FAT32 / exFAT のテストイメージ生成スクリプトを追加する
  (「作成→ファイル配置→削除」「クイックフォーマット」「断片化配置」の各シナリオ)。
