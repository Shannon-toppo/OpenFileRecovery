# Open File Recovery 開発プラン

USBメモリとSDカードを主対象としたデータ復旧ソフトの設計ドキュメント。実装者(Claude Opus / Sonnet)はこのドキュメントを仕様として扱うこと。判断に迷ったら「6. 安全原則」を最優先する。

## 1. 確定済みの方針

| 項目 | 決定 |
|---|---|
| UI | Tauri v2(フロントは TypeScript + 任意の軽量フレームワーク。Svelte を推奨) |
| コア | Rust(cargo workspace で複数クレートに分割) |
| 対応OS | Windows 10/11 (x64), macOS 13+ (Apple Silicon / Intel) |
| 削除復元・構造復元の対象FS | FAT32 と exFAT |
| カービング(FS非依存の直接探索) | 全デバイスに適用可(FSを問わない) |
| ライセンス | MIT。**GPL/LGPLコードの流用・リンクは一切禁止**(アルゴリズムを文書から学んで自前実装するのは可) |
| 依存の方針 | ffmpeg等の外部バイナリは同梱しない。軽量化とライセンス回避のため、必要なパーサは自前で書く |

## 2. 要件と実現方法の対応

| 要件 | 実現方法 | 該当セクション |
|---|---|---|
| Windows/Mac両対応 | Rustコア + Tauri。デバイスアクセスだけOS別実装 | 5.1 |
| フォーマット・削除したデータの復元 | FAT32/exFAT の削除エントリ解析 + シグネチャカービング | 5.3, 5.4 |
| 壊れかけたメディアからの吸い出し(自動リトライ) | ddrescue方式のイメージングエンジンを自前実装 | 5.2 |
| データとフォルダ構造をそのままコピー | 論理コピー機能(リトライ付き)+ イメージ経由の展開 | 5.5 |
| 高速・軽量 | 生デバイスIO、スレッド分離、SIMD検索。バイナリは単体配布 | 5.7 |
| JPEG/PNG/MP4/AVI の破損修復 | フォーマット別の修復モジュール | 5.6 |

注意: 要件にある「コーデック」は正確にはコンテナ/ファイルフォーマットの修復を指す。H.264等のコーデックビットストリーム自体の欠損は原理的に補えないので、本ソフトの修復対象は「コンテナ構造の再建と、デコード可能な範囲のサルベージ」と定義する。

## 3. リポジトリ構成

```
Open_File_Recovery/
├── PLAN.md                  # このファイル
├── Cargo.toml               # workspace ルート
├── crates/
│   ├── ofr-device/          # 生デバイスアクセス(OS別)。全クレートの土台
│   ├── ofr-image/           # ddrescue方式イメージングエンジン + mapfile
│   ├── ofr-fat/             # FAT32 パーサ(削除エントリ復元含む)
│   ├── ofr-exfat/           # exFAT パーサ(同上)
│   ├── ofr-carve/           # シグネチャカービング
│   ├── ofr-repair/          # JPEG/PNG/MP4/AVI 修復
│   ├── ofr-copy/            # 論理コピー(構造保持ミラー)
│   ├── ofr-core/            # 上記を束ねる公開API。進捗イベント、ジョブ管理
│   └── ofr-cli/             # CLI(開発・デバッグ・ヘッドレス用。GUIより先に動かす)
├── app/                     # Tauri アプリ(ofr-core を呼ぶ)
│   ├── src-tauri/
│   └── src/                 # フロントエンド
└── testdata/                # テストイメージ生成スクリプトと生成物(生成物はgitignore)
```

依存クレートは MIT/Apache-2.0 のものに限る。想定: `windows`(win32 FFI)、`libc` または `nix`(macOS)、`memchr`、`crc32fast`、`serde`、`thiserror`、`tracing`、`image`(修復結果の検証デコード用)、`crossbeam-channel`。tokio は使わない(生デバイスIOは同期のほうが速く単純。並列はスレッドで組む)。

## 4. アーキテクチャ

```
[Tauri フロント (TS)]
      │  invoke / event (進捗・発見ファイル通知)
[Tauri コマンド層 (src-tauri)]
      │
[ofr-core: ジョブAPI]───┬─ ofr-image ─┐
      │                ├─ ofr-fat    ├── ofr-device (trait Device)
      │                ├─ ofr-exfat  │      ├─ WindowsDevice
      │                ├─ ofr-carve ─┘      ├─ MacDevice
      │                ├─ ofr-copy          └─ FileDevice (イメージ/テスト用)
      │                └─ ofr-repair
```

設計上の要点:

- `ofr-device` が `trait Device { read_at, len, block_size, ... }` を定義し、実デバイスもイメージファイルもモックも同じ型で扱う。これで上位クレートは全てOS非依存かつユニットテスト可能になる。
- 解析系(fat/exfat/carve)は「デバイス直読み」と「イメージファイル読み」のどちらでも動く。壊れかけメディアでは先にイメージを取り、イメージに対して解析する流れを推奨フローとする(6章)。
- GUIとコアの間は `ofr-core` のジョブAPIだけで通信する。ジョブは開始するとスレッドで走り、進捗・発見ファイル・エラーを channel 経由のイベントで通知する。キャンセルは `AtomicBool` フラグ。

## 5. 主要コンポーネント設計

### 5.1 ofr-device: 生デバイスアクセス

- **Windows**: `CreateFileW("\\\\.\\PhysicalDriveN")` を `GENERIC_READ` のみで開く。`FILE_FLAG_NO_BUFFERING` を使うため読み込みはセクタ境界に整列させる(整列はこのクレート内で吸収し、上位には任意オフセットのreadを見せる)。デバイス列挙は SetupAPI または `IOCTL_STORAGE_GET_DEVICE_NUMBER` 系で行い、リムーバブルかどうか、サイズ、ボリュームレターを取得する。管理者権限が必要なので、Tauri アプリは manifest で `requireAdministrator` を指定する。
- **macOS**: `/dev/rdiskN`(rawデバイス)を `O_RDONLY` で開く。マウント中は開けないので、スキャン開始前に `diskutil unmountDisk` を呼ぶ(ユーザーに確認してから)。root権限が必要になるため、GUI からは `osascript` の administrator privileges 経由でヘルパーを起動するか、初期実装では「sudo で CLI を実行」という案内でよい。権限まわりは Phase 1 で最小限、Phase 6 で仕上げる。
- デバイス列挙には安全装置を入れる: システムディスク(OS起動ディスク)は一覧に出すが選択不可にする。
- `FileDevice` はただのファイルバックエンド。テストとイメージ解析の両方で使う。
- **書き込みAPIをこの trait に作らないこと。** 復旧元への書き込み経路をコンパイル時点で存在させないのが狙い。

### 5.2 ofr-image: イメージングエンジン(壊れかけメディア対応の心臓部)

GNU ddrescue のアルゴリズムを自前実装する(ddrescue のコードは GPL なので見ない。アルゴリズムは公開マニュアルの記述に基づく)。

- **多段パス方式**:
  1. **Copy pass**: 大きめのブロック(例: 1MiB)で先頭から読む。エラーが出た領域は記録してスキップし、まず読める領域を全部確保する。読み込み速度が閾値を下回る領域も後回しにできるとよい(オプション)。
  2. **Trim pass**: 失敗領域の端からセクタ単位で前進・後退して読み、不良域の境界を狭める。
  3. **Scrape pass**: 残った不良域内をセクタ単位で総当たりする。
  4. **Retry pass**: それでも読めない領域を指定回数リトライする(回数と待ち時間は設定可能。既定はリトライ3回)。
- **mapfile**: どの領域が「取得済み/未試行/不良」かを記録するファイル。GNU ddrescue の mapfile 形式と互換にする(テキスト形式で仕様が公開されている)。互換にしておくと、ユーザーが本家 ddrescue と行き来できるし、中断・再開もこのファイルだけで実現できる。
- **リトライ戦略**: IOエラー時は指数バックオフ(例: 100ms→500ms→2s)を挟んで再試行。デバイスハンドルの開き直しも1回試す(USBコントローラが一時的に固まるケースへの対処)。それでも駄目なら不良マークして先へ進む。**1セクタに固執して全体を止めないことが最重要。**
- 進捗イベント: 取得済みバイト数、不良バイト数、現在速度、推定残り時間、mapfile の領域マップ(GUIで帯グラフ表示する)。
- 出力は単純な raw イメージ(.img)。スパースファイルとして書き、未取得領域はゼロのままにする。

### 5.3 ofr-fat / ofr-exfat: ファイルシステム解析

読み取り専用パーサ。マウントはしない。破損に耐えることが通常のFS実装との違いで、パース中のエラーは全て「その項目をスキップして続行」で処理する。panic 禁止。

- **FAT32**:
  - ブートセクタが壊れていてもバックアップブートセクタ(セクタ6)や、FAT表・クラスタサイズの推定で続行を試みる。
  - 通常走査でディレクトリツリーを構築。削除ファイルは先頭バイト 0xE5 のエントリとして検出し、LFNエントリから元のファイル名を可能な範囲で復元する。
  - 削除ファイルの本体は開始クラスタから連続配置と仮定して回収する(FATチェーンは削除時に解放されているため)。断片化していた場合は壊れたデータになるが、それは修復モジュール(5.6)へ回す。
  - ルートから辿れない孤立ディレクトリクラスタも全クラスタ走査で拾い、「Lost+Found」ツリーとして提示する。フォーマット後の復元はこの機能が主力になる(クイックフォーマットはFAT表とルートを消すだけで、サブディレクトリのクラスタは大半残っている)。
- **exFAT**: 同様の方針。削除エントリは EntryType の InUse ビット(bit7)が落ちたものとして検出。NoFatChain ビットが立っていた(連続配置だった)ファイルはサイズ情報から高精度で回収できる。allocation bitmap は参考情報として使う。
- 出力は共通の中間表現 `RecoveredEntry { path, name, size, timestamps, clusters, status }`。status は Intact / Deleted / Orphaned / Damaged。GUI はこのツリーを表示し、ユーザーが選んだものを復元する。

### 5.4 ofr-carve: シグネチャカービング

FS情報が全滅している場合(フルフォーマット、FS破損)の最終手段。デバイス/イメージを先頭から走査してファイル形式のシグネチャを探す。

- 走査は `memchr`(SIMD)でマジックバイト候補を探し、ヒットしたらフォーマット別バリデータで確認する二段構え。クラスタ境界(通常はファイル先頭が来る位置)を優先してスキャンし、精度と速度を稼ぐ。
- 対応フォーマット(初期セット): JPEG, PNG, GIF, HEIC, MP4/MOV, AVI, WAV, MP3, ZIP(docx/xlsx含む), PDF。定義はテーブル駆動にして追加しやすくする。
- 終端検出: JPEGはEOIマーカー、PNGはIENDチャンク、MP4/AVI/ZIPなどはヘッダ内のサイズ情報を辿って正確な終端を計算する。サイズ情報が壊れている場合は次のシグネチャまでを上限とした最大サイズで切り出す。
- カービング結果はファイル名が失われる(`carved_000123.jpg` のような連番になる)。元のファイル名を取り戻す手段はない。撮影日時などをメタデータ(Exif等)から拾って連番名に反映する処理は、フェーズ表の Phase 3「Exifメタデータ抽出」がこれで、Phase 3 の後半に実装する(Phase 5 の修復モジュールとは無関係)。

### 5.5 ofr-copy: 構造保持コピー

要件「デバイスのデータとフォルダ構造をそのままコピー」に対応する。2モード用意する。

1. **論理コピー**: デバイスのツリーを宛先フォルダへそのままミラーする。読み出し経路は2つあり、OSにマウントされているデバイスはOSのファイルAPIで読み、マウントできない(が生読みはできる)デバイスは ofr-fat/exfat の直読みで代替する。どちらの経路でも出力は同じミラーツリーになる。各ファイルの読み込みにも 5.2 と同じリトライ戦略を適用し、部分的にしか読めなかったファイルは読めた分を保存した上でレポートに記録する。タイムスタンプは可能な範囲で維持する。
2. **イメージからの展開**: 5.2 で取ったイメージを 5.3 のパーサで読み、ツリーを宛先へ展開する。壊れかけメディアではこちらが推奨ルート。

どちらも完了時に結果レポート(全ファイルの成否とエラー内容のJSON + 人間向けサマリ)を宛先に書き出す。

### 5.6 ofr-repair: 破損ファイル修復

復元したファイルが開けない場合の修復。**修復は必ずコピーに対して行い、元ファイルは残す。** 各フォーマットで実現可能なラインが違うので、下記を仕様とする。

- **JPEG**:
  - ヘッダ(SOI〜SOS)が壊れて本体が残っているケース: 同じカメラ/アプリで撮れた正常ファイルを参照ヘッダとして移植する。参照がなければ標準量子化テーブル+標準ハフマンテーブルで組み立てて試す。
  - 途中で切れているケース: デコードできた行までを画像として確定し、残りはグレーで埋めて保存する。リスタートマーカー(DRI/RSTn)があれば破損箇所以降のマーカーから再同期して後半も救う。
  - 検証: `image` クレートでデコードが通るか、極端な色破綻がないか(簡易ヒューリスティック)を確認して成否を返す。
- **PNG**: チャンク構造を走査し、CRC不一致は再計算で修正、長さフィールドの破損は次チャンクのシグネチャ探索で復元する。IDATの中身自体が壊れている場合、zlibストリームをデコードできた所まで部分復元する。
- **MP4/MOV**:
  - 最重要ケースは moov ボックス(インデックス)の欠損・破損。mdat(実データ)だけが残る。
  - 対処は参照ファイル方式: 同じ機器・同じ設定で録画された正常ファイルをユーザーに用意してもらい、その moov 構造とコーデック設定を雛形に、mdat 内の NAL ユニット境界(H.264/H.265 の長さプレフィックス)を走査してサンプルテーブルを再構築する。untrunc というGPLツールと同じ発想だが、コードは見ずに一から実装する。
  - 参照ファイルがない場合は精度が大きく落ちる旨をUIで明示し、H.264 Annex-B 変換での取り出しなど限定的な救済に留める。
- **AVI**: RIFF チャンクを走査して idx1 インデックスを再構築する。ストリームヘッダ(avih/strh)が壊れている場合はチャンク実測値から推定する。AVIは構造が単純なので4形式の中では最も修復しやすい。
- 共通API: `fn repair(input, reference: Option<Path>, output) -> RepairReport`。Report には何をどこまで直せたか、検証結果、残っている問題を含める。
- ffmpeg には一切依存しない。同梱しないだけでなく、ユーザーの環境にインストール済みの ffmpeg を検出して呼び出す実装も禁止(挙動がバージョン依存になり、テスト不能になるため)。動画の自動検証はコンテナ整合性チェック(全サンプルがmdat範囲内を指しているか等)までとする。実視聴での確認はユーザーに委ねる。
- 4形式の実装順序 JPEG → PNG → AVI → MP4 の理由: JPEGとPNGは `image` クレートで修復結果を自動検証できるので回帰テストを最初に確立しやすい。AVIは動画2形式のうち構造が単純な方。MP4は参照ファイル方式の実装が最も重いので、他形式でテスト基盤と修復APIの形を固めてから最後に取り組む。

### 5.7 パフォーマンス方針

- IOは専用スレッド1本(壊れかけデバイスへの並列アクセスはコントローラを詰まらせるので**シーケンシャル固定**)。解析・シグネチャ判定・修復はワーカースレッドプールで並列化し、IOスレッドとは channel で繋ぐ。健全なイメージファイルに対する解析は並列読みしてよい。
- 読み込み単位は既定1MiB。デバイスの応答を見て動的に縮小(エラー多発時)する。
- 目標値: 健全なUSB 3.0メモリのイメージングでデバイス上限速度の90%以上。カービングはNVMe上のイメージに対して 500MB/s 以上。GUIのアイドル時メモリは200MB以下、インストーラは20MB以下。
- 進捗イベントの発火は100ms間隔に間引く(イベント洪水でGUIが固まるのを防ぐ)。

## 6. 安全原則(実装上の絶対ルール)

1. 復旧元デバイスには何があっても書き込まない。`ofr-device` に書き込みAPIを作らない。
2. 復元先が復旧元と同じデバイス上にある場合はエラーにする(ボリュームレター/マウントポイントで判定)。
3. OS起動ディスクは復旧元として選択不可。
4. 壊れかけメディアはアクセスのたびに劣化しうる。GUIは「まずイメージを取得→イメージに対して解析」を標準フローとして誘導し、デバイス直接解析は健全なデバイス向けの近道として提供する。
5. 全ての解析コードは不正なバイト列で panic しないこと(fuzz でこれを検証する)。エラーはスキップと記録で処理する。

## 7. GUI 設計(Tauri)

画面フローは1本道にする。復旧ソフトのユーザーはトラブル中で余裕がないので、選択肢を並べるより順番に進める構成が向く。

1. **デバイス選択**: リムーバブルデバイス一覧(名前、容量、FS、S/N)。システムディスクはグレーアウト。
2. **モード選択**: 「削除したファイルを探す」「フォーマットしたデバイスから探す」「壊れかけたデバイスを丸ごと吸い出す」「フォルダごとコピーする」の4つを日常語で提示。内部的には fat/exfat解析、解析+カービング、イメージング、論理コピーへマッピングする。
3. **スキャン画面**: 進捗バー、速度、エラー数。イメージング時は領域マップ(取得済み/不良/未試行の帯グラフ)を表示。中断・再開可能。
4. **結果ツリー**: フォルダ構造ツリー + カービング結果。画像はサムネイルプレビュー(これが復旧ソフトの信頼感を決める)。状態バッジ(無傷/削除済み/破損)。チェックボックスで選択。
5. **復元**: 宛先選択 → 実行 → レポート表示。破損ファイルにはここで「修復を試す」ボタンを出す(参照ファイル指定UI付き)。

表示言語は日本語と英語の2言語対応(文字列はリソースファイルに分離)。

## 8. 実装フェーズ

各フェーズは前のフェーズに依存する。完了条件を満たしてから次へ進むこと。

| Phase | 内容 | 完了条件 |
|---|---|---|
| 0 | workspace 雛形、`trait Device`、`FileDevice`、モックデバイス(エラー注入可能)、CI設定 | モックに対するユニットテストが両OSのCIで通る |
| 1 | Windows/Mac の実デバイス実装 + デバイス列挙 + ofr-image(全パス + mapfile + リトライ) + CLI (`ofr list`, `ofr image`) | 実機USBメモリをイメージング完走。エラー注入モックで不良スキップ・再開・mapfile互換を確認 |
| 2 | ofr-fat / ofr-exfat(通常走査 + 削除復元 + 孤立クラスタ) + CLI (`ofr scan`, `ofr restore`) | テストイメージで「削除→復元」「クイックフォーマット→復元」がファイル内容一致で成功 |
| 3 | ofr-carve(全対応フォーマット) + Exifメタデータ抽出 | テストイメージに埋めた既知ファイル群の90%以上を正しい境界で切り出せる |
| 4 | ofr-copy(論理コピー + イメージ展開 + レポート) | エラー注入デバイスからのコピーで、読めたファイルが全て宛先に揃いレポートと一致する |
| 5 | ofr-repair(JPEG → PNG → AVI → MP4 の順。順序の理由は5.6参照) | 破損サンプル集(9章で作る)に対し、JPEG/PNG/AVIは既定ケースで修復成功。MP4は参照ファイルありで再生可能なファイルを出力(「再生可能」の判定は人間がプレイヤーで開いて確認する手動テストでよい。自動テストはコンテナ整合性チェックまで) |
| 6 | Tauri GUI 全画面、権限昇格の仕上げ、i18n、インストーラ(msi/dmg)、コード署名は保留可 | 実機で「フォーマットしたSDカードから写真を選んで復元」がGUIだけで完了する |

CLI(ofr-cli)を常にGUIより先行させる。全機能はまずCLIで動かして検証し、GUIは薄いラッパーに保つ。

クレートの作成タイミング: Phase 0 で作るのは workspace ルートと ofr-device(と、その中の FileDevice / MockDevice)だけでよい。3章のツリーにある他のクレートは、それを使うフェーズの開始時に追加する。空のクレートを Phase 0 で全部並べておく必要はない。

## 9. テスト戦略

- **テストイメージ生成**: `testdata/` にスクリプトを置き、小さな FAT32/exFAT イメージを機械生成する(macOSなら hdiutil + newfs、Windowsなら diskpart + format をスクリプト化。もしくは Rust で直接FS構造を書くジェネレータを作るとCIで完結する)。「作成→ファイル配置→削除」「クイックフォーマット」「断片化配置」の各シナリオを用意する。
- **エラー注入**: `MockDevice` に不良セクタ範囲、一時的エラー(N回目で成功)、遅延を設定できるようにし、ofr-image のリトライとパス制御をユニットテストする。実機の壊れたUSBメモリはCIに置けないので、この抽象化がテストの生命線になる。
- **破損サンプル集**: 正常な JPEG/PNG/MP4/AVI から「ヘッダ破壊」「途中切断」「moov削除」「idx1削除」などをスクリプトで生成し、ofr-repair の回帰テストにする。
- **fuzz**: cargo-fuzz で fat/exfat/carve/repair のパーサに対して panic しないことを継続的に確認する。
- CI は GitHub Actions で windows-latest / macos-latest の両方を回す。実デバイステストだけは手動チェックリストにする。

## 10. 既知のリスクと注意点

- **権限昇格がTauriで一番の難所。** Windows は manifest 指定で解決するが、macOS のGUIからの root 取得は素直な方法がない。Phase 1〜5 は sudo 実行の CLI で全機能が使える状態を保ち、GUI の昇格は Phase 6 の課題として切り離してある。ここで詰まったら「macOS版はGUIから特権ヘルパーを呼ぶ」設計に切り替える。
- **物理障害には踏み込まない。** コントローラが認識しない、デバイスとして見えない個体はソフトウェアでは救えない。対象は「OSからブロックデバイスとして見えるが読み出しが不安定」な個体まで。READMEに明記する。
- **SSDのTRIM**: TRIM済み領域は復元不能。USBメモリとSDカードは通常TRIMが効かないので主対象では問題になりにくいが、外付けSSDユーザー向けに注意書きを出す。
- **断片化した削除ファイル**: FAT系は削除時にチェーン情報が消えるため、断片化していたファイルの完全復元は原理的に保証できない。連続配置仮定で回収し、壊れていたら修復モジュールに回す、が本ソフトの立場。
- **MP4修復の期待値管理**: 参照ファイルなしの moov 再構築は市販ソフトでも成功率が低い。UIで正直に伝える。
- **配布時の署名**: macOS の公証(notarization)と Windows の署名がないと警告が出る。MIT OSS なので当面は「未署名 + READMEで手順案内」でよい。Phase 6 で判断する。
